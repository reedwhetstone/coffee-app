# E2E Test Configuration
# Copy this to .env.test and fill in your test account credentials

# Test account for E2E testing (admin magic-link auth)
E2E_TEST_EMAIL=your-test-email@example.com
E2E_TEST_USER_ID=your-test-user-uuid

# Supabase service role key (required to generate magic links for E2E auth)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Public Supabase config required by SvelteKit static env imports during validation
PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Coffee-app signs short-lived account-deletion reauthentication assertions.
# PRIVATE_KEYS is a JSON object keyed by kid; keep private JWK material server-only.
ACCOUNT_DELETION_REAUTH_ISSUER=https://www.purveyors.io
ACCOUNT_DELETION_REAUTH_AUDIENCE=https://api.purveyors.io/v1/account-deletion
ACCOUNT_DELETION_REAUTH_PRIVATE_KEYS=replace-with-json-private-key-ring
ACCOUNT_DELETION_REAUTH_ACTIVE_KID=replace-with-active-kid

# Base URL for tests (defaults to localhost:5173)
PLAYWRIGHT_BASE_URL=http://localhost:5173

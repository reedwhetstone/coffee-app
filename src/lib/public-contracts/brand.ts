/**
 * Shared brand facts for public copy and SEO metadata.
 *
 * Marketing copy, meta descriptions, and stat tiles must all cite the same
 * numbers. Update these constants instead of editing counts inline.
 */

/** Canonical supplier-coverage claim used across marketing copy and SEO. */
export const IMPORTER_COUNT_LABEL = '40+';

/** Canonical catalog-size claim used in SEO metadata. */
export const CATALOG_SIZE_LABEL = '1,200+';
